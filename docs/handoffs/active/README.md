# Active build handoffs

Temporary **implement** packets for WQ rows currently **Active** on [`WORK_QUEUE.md`](../../WORK_QUEUE.md).

Naming: `HANDOFF_WQnnn_<slug>.md` · registry table required.

**When the WQ row moves to Done:** update the Done log with evidence, then **delete this file** — no archive.

*(empty — no active process slices)*
