# BSOD Analyzer v5 maintenance line

Version **5.4.13** is the final v5 feature release: **AMD logo fix only** (reference PNG).

All driver-catalog / MSCatalogLTS / accuracy work lives in **v6** (`BSODAnalyzer_v6\`).

## Build v5 maintenance

```bat
build_and_deploy_v5.bat
```

(`amd.png` is built automatically from `assets/vendor_icons/amd_reference.png` in the repo.)

## Build v6 (active development)

```bat
build_and_deploy_v6.bat
```

Both lines bundle **`assets/vendor_icons/amd.png`** (same approved emblem).
